<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Music Tracks</title>
  <link rel="stylesheet" href="index.css" />
</head>
<body>
  <div class="container">
    <header>
      <h1 data-testid="tracks-header"><span class="note">🎵</span> Music Tracks</h1>
      <div class="controls">
        <input type="text" id="search-input" data-testid="search-input" placeholder="Search by title, artist, genre..." />
        <select id="sort-select" data-testid="sort-select">
          <option value="">Sort by...</option>
          <option value="title">Title</option>
          <option value="artist">Artist</option>
          <option value="genre">Genre</option>
        </select>
        <input type="text" id="filter-genre" data-testid="filter-genre" placeholder="Filter by genre" />
        <input type="text" id="filter-artist" data-testid="filter-artist" placeholder="Filter by artist" />
        <button id="create-track-button" data-testid="create-track-button">+ Create Track</button>
        <button id="delete-selected-button" class="danger-button" data-testid="bulk-delete-button">Delete Selected</button>
      </div>
    </header>

    <ul id="track-list" class="track-list" data-testid="track-list"></ul>

    <div class="pagination-wrapper">
      <button id="pagination-prev" data-testid="pagination-prev">Prev</button>
      <div id="pagination" class="pagination" data-testid="pagination"></div>
      <button id="pagination-next" data-testid="pagination-next">Next</button>
    </div>

    <div id="modal" class="modal hidden" data-testid="modal">
      <form id="track-form" data-testid="track-form" class="form">
        <input type="text" id="input-title" data-testid="input-title" placeholder="Track Title" required />
        <div class="error-message" data-testid="error-title"></div>

        <input type="text" id="input-artist" data-testid="input-artist" placeholder="Artist Name" required />
        <div class="error-message" data-testid="error-artist"></div>

        <input type="text" id="input-album" data-testid="input-album" placeholder="Album" />
        <input type="text" id="input-cover-image" data-testid="input-cover-image" placeholder="Cover Image URL" />

        <div id="genre-tags" class="genre-tags" data-testid="genre-tags"></div>
        <div class="genre-input-wrapper">
          <input type="text" id="input-genre" data-testid="genre-selector" placeholder="Add genre..." />
          <button type="button" id="add-genre" data-testid="add-genre-button">+</button>
        </div>
        <div class="error-message" data-testid="error-genre"></div>

        <input type="file" id="input-audio" accept="audio/*" data-testid="input-audio" />
        <div id="audio-preview-container" class="audio-preview hidden" data-testid="audio-preview-container">
          <div class="audio-wrapper">
            <audio id="audio-preview" class="audio-player" controls data-testid="audio-preview"></audio>
          </div>
          <button type="button" id="remove-audio" class="cancel-button" data-testid="remove-audio-button">Remove Audio</button>
        </div>

        <button type="submit" id="submit-button" data-testid="submit-button">Save Track</button>
      </form>
      <button type="button" onclick="closeModal()" class="cancel-button" data-testid="cancel-button">Cancel</button>
    </div>

    <!-- Confirm Delete Dialog -->
    <div id="confirm-dialog" class="modal hidden" data-testid="confirm-dialog">
      <div class="form" role="dialog">
        <p>Are you sure you want to delete the selected tracks?</p>
        <div class="confirm-buttons">
          <button id="confirm-delete" data-testid="confirm-delete">Yes, delete</button>
          <button id="cancel-delete" data-testid="cancel-delete">Cancel</button>
        </div>
      </div>
    </div>

    <!-- Toast Notifications -->
    <div id="toast-container" data-testid="toast-container"></div>

    <!-- Loading States -->
    <div id="loading-indicator" data-testid="loading-indicator" class="loading hidden">
      <div class="spinner"></div>
      Loading...
    </div>

    <div id="loading-tracks" data-testid="loading-tracks" class="loading hidden">
      <div class="spinner"></div>
      Loading tracks...
    </div>

    <!-- Track Counter and Duration Summary -->
    <div class="track-summary">
      <p data-testid="total-tracks">Total tracks: <span id="track-count">0</span></p>
      <p data-testid="total-duration">Total duration: <span id="track-duration">0:00</span></p>
    </div>
  </div>
  <script src="script.js"></script>
</body>
</html>
